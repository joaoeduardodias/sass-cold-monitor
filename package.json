{
  "name": "sass-cold-monitor",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:collector": "pnpm --filter @cold-monitor/collector dev",
    "dist:collector": "pnpm --filter @cold-monitor/collector dist",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=18"
  }
}
